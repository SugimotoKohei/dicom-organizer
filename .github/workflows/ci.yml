name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }} / Python 3.11
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Check lockfile
        run: uv lock --check

      - name: Install dependencies
        run: uv sync --locked

      - name: Run tests
        run: uv run python -m pytest

      - name: Run ruff
        run: uv run ruff check

      - name: Build package
        run: uv build

      - name: CLI smoke tests
        run: |
          uv run dicom-organizer --help
          uv run python -c "import dicom_organizer; import dicom_organizer.core"
          uv run dicom-organizer-gui --help

      - name: Synthetic workflow smoke test
        run: uv run python examples/synthetic_quickstart.py
